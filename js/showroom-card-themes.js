// The previous card themes targeted the retired compact header.
// Keep this module boundary for the new expanded-header theme collection.
export const CARD_THEME_ITEMS = Object.freeze([]);

export default CARD_THEME_ITEMS;
